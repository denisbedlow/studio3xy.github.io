---
import type { MarkdownHeading } from 'astro';
import RightTocIsland from './RightToc.svelte';

interface Props {
  headings: MarkdownHeading[];
}

const { headings } = Astro.props;
---

<RightTocIsland {headings} client:load />
